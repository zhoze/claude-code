# Copy to `.env` (gitignored) or export in your shell / store as a CI secret.
# NEVER commit the real key. Get one at https://financialmodelingprep.com
FMP_KEY=your_financialmodelingprep_api_key_here

# Usage:
#   export $(grep -v '^#' .env | xargs)   # load into shell
#   python3 refresh_sp500.py              # re-pull the full S&P 500
